/**
 * Patient List tab.
 * Criteria-based patient lists with compare mode. Query builder (collapsible)
 * and a table of patients with difference type, demographics, Death/SAE/AE to DC.
 */
import { PatientListTab as PatientListTabImpl } from "../components/PatientListTab";

export function PatientListTab() {
  return <PatientListTabImpl />;
}
